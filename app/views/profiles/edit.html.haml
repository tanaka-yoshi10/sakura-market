= simple_form_for([:admin, @current_user], url: '/profiles', method: :patch) do |f|
  = f.error_notification
  = f.input :name, label: '名前', input_html: { size: 50, minlength: 1 }
  = f.input :email, label: 'メールアドレス', input_html: { size: 30, minlength: 1 }
  = f.button :submit, '更新する'


